{
  "name": "kg-registration-system",
  "version": "1.0.0",
  "description": "Registration system with Firebase profile verification and MySQL data storage",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "db:migrate": "mysql -u root -p registration_db < database/schema.sql"
  },
  "keywords": ["firebase", "mysql", "registration", "authentication"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.10.0",
    "mysql2": "^3.4.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0"
  }
}